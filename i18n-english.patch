diff --git a/alerts/alerts.py b/alerts/alerts.py
index 1234567..abcdefg 100644
--- a/alerts/alerts.py
+++ b/alerts/alerts.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # -*- coding: utf-8 -*-
 """
-Alert-tjänst med cooldown i PostgreSQL
+Alert service with cooldown in PostgreSQL
 """
 
 import asyncio
@@ -17,7 +17,7 @@ from psycopg2.pool import ThreadedConnectionPool
 import psycopg2.extras
 from psycopg2.pool import ThreadedConnectionPool
 
-# === Konfiguration ===
+# === Configuration ===
 LOG_DIR = "/logs"
 DB_HOST = os.getenv('DB_HOST', 'postgres')
 DB_NAME = os.getenv('DB_NAME', 'scraper')
@@ -48,8 +48,8 @@ alerts_sent = 0
 db_pool = None
 
 
-# === Hjälpfunktion för secrets ===
+# === Helper function for secrets ===
 def read_secret(env_var, default=""):
-    """Läs secret från fil eller env"""
+    """Read secret from file or env"""
     path = os.getenv(f"{env_var}_FILE")
     if path and os.path.exists(path):
         with open(path) as f:
@@ -77,7 +77,7 @@ def return_db(conn):
 
 
 def get_webhook():
-    """Hämta Discord webhook från secret eller env"""
+    """Get Discord webhook from secret or env"""
     return read_secret("DISCORD_WEBHOOK")
 
 
@@ -87,15 +87,15 @@ def send_discord(webhook, title, old_price, new_price, url):
     
     payload = {
         "embeds": [{
-            "title": "💸 Prisfall!",
+            "title": "💸 Price Drop!",
             "description": f"**{title}**",
             "color": 16711680,
             "fields": [
-                {"name": "Gammalt", "value": f"{old_price:,} kr".replace(",", " "), "inline": True},
-                {"name": "Nytt", "value": f"{new_price:,} kr".replace(",", " "), "inline": True},
-                {"name": "Nedgång", "value": f"-{drop:,} kr ({percent}%)".replace(",", " "), "inline": True},
-                {"name": "Länk", "value": url}
+                {"name": "Old", "value": f"{old_price:,} kr".replace(",", " "), "inline": True},
+                {"name": "New", "value": f"{new_price:,} kr".replace(",", " "), "inline": True},
+                {"name": "Drop", "value": f"-{drop:,} kr ({percent}%)".replace(",", " "), "inline": True},
+                {"name": "Link", "value": url}
             ]
         }]
     }
@@ -111,7 +111,7 @@ async def check_drops():
     
     webhook = get_webhook()
     if not webhook:
-        logger.error("Ingen webhook konfigurerad")
+        logger.error("No webhook configured")
         return 0
     
     conn = get_db()
@@ -164,7 +164,7 @@ async def check_drops():
                 if send_discord(webhook, row['title'], row['old_price'], row['new_price'], row['url']):
                     alerts_this_run += 1
                     alerts_sent += 1
-                    logger.info(f"Alert: {row['title'][:50]}...")
+                    logger.info(f"Alert sent: {row['title'][:50]}...")
                     await asyncio.sleep(1)
         
         conn.commit()
@@ -175,13 +175,13 @@ async def check_drops():
 
 
 async def alerts_loop():
-    logger.info(f"Alerts startade. Intervall: {CHECK_INTERVAL}s")
+    logger.info(f"Alerts started. Interval: {CHECK_INTERVAL}s")
     
     while not shutdown_event.is_set():
         try:
             sent = await check_drops()
             if sent:
-                logger.info(f"Skickade {sent} alerts")
+                logger.info(f"Sent {sent} alerts")
         except Exception as e:
             logger.error(f"Fel: {e}")
         
@@ -190,7 +190,7 @@ async def alerts_loop():
         except asyncio.TimeoutError:
             pass
     
-    logger.info(f"Alerts avslutade. Totalt: {alerts_sent}")
+    logger.info(f"Alerts stopped. Total sent: {alerts_sent}")
 
 
 def signal_handler():
diff --git a/alerts/entrypoint.sh b/alerts/entrypoint.sh
index 2345678..bcdefgh 100644
--- a/alerts/entrypoint.sh
+++ b/alerts/entrypoint.sh
@@ -3,7 +3,7 @@ set -e
 
 echo "🔧 Fixing permissions for UID:GID = ${PUID}:${PGID}"
 
-# Skapa /logs med rätt rättigheter
+# Create /logs with correct permissions
 mkdir -p /logs
 chown -R ${PUID}:${PGID} /logs 2>/dev/null || true
 
diff --git a/api/api.py b/api/api.py
index 3456789..cdefghi 100644
--- a/api/api.py
+++ b/api/api.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # -*- coding: utf-8 -*-
 """
-PostgreSQL-baserat API - Produktionsversion med connection pooling
+PostgreSQL-based API - Production version with connection pooling
 """
 
 import os
@@ -28,7 +28,7 @@ ALLOWED_ORIGINS = os.getenv('ALLOWED_ORIGINS', 'https://scraper.denied.se').spl
 logger = logging.getLogger(__name__)
 
 def read_secret(env_var, default=""):
-    """Läs secret från fil eller env"""
+    """Read secret from file or env"""
     path = os.getenv(f"{env_var}_FILE")
     if path and os.path.exists(path):
         with open(path) as f:
@@ -55,7 +55,7 @@ def return_db(conn):
 
 app = FastAPI(
     title="Web Scraper API",
-    description="Produktions-API för prisbevakning",
+    description="Production API for price monitoring",
     version="4.0.0",
     docs_url="/docs",
     redoc_url="/redoc"
diff --git a/scraper/entrypoint.sh b/scraper/entrypoint.sh
index 4567890..defghij 100644
--- a/scraper/entrypoint.sh
+++ b/scraper/entrypoint.sh
@@ -3,7 +3,7 @@ set -e
 
 export PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
 
-# Ladda ner Chromium om det saknas (första start med tom volym)
+# Download Chromium if missing (first start with empty volume)
 if [ ! -d "/ms-playwright/chromium-"* ] 2>/dev/null; then
     echo "📦 Downloading Chromium to persistent volume..."
     python -m playwright install chromium
diff --git a/scraper/scraper.py b/scraper/scraper.py
index 5678901..efghijk 100644
--- a/scraper/scraper.py
+++ b/scraper/scraper.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # -*- coding: utf-8 -*-
 """
-PostgreSQL-baserad multi-site scraper - Produktionsversion
+PostgreSQL-based multi-site scraper - Production version
 med proxy-stöd, retry/backoff och periodic flush
 """
 
@@ -25,7 +25,7 @@ from psycopg2.pool import ThreadedConnectionPool
 
 app = Flask(__name__)
 
-# === Konfiguration ===
+# === Configuration ===
 LOG_DIR = "/logs"
 MAX_CONCURRENT = int(os.getenv('CONCURRENT_PAGES', '2'))
 HEADLESS = os.getenv('HEADLESS', 'true').lower() == 'true'
@@ -49,7 +49,7 @@ write_lock = asyncio.Lock()
 
 
 def read_secret(env_var, default=""):
-    """Läs secret från fil eller env"""
+    """Read secret from file or env"""
     path = os.getenv(f"{env_var}_FILE")
     if path and os.path.exists(path):
         with open(path) as f:
@@ -73,7 +73,7 @@ def init_db_pool():
 
 
 def get_db():
-    """Hämta anslutning från poolen"""
+    """Get connection from pool"""
     return db_pool.getconn()
 
 
@@ -82,7 +82,7 @@ def return_db(conn):
 
 
 def init_db():
-    """Initiera PostgreSQL-databas"""
+    """Initialize PostgreSQL database"""
     conn = get_db()
     cur = conn.cursor()
     
@@ -117,7 +117,7 @@ def init_db():
         'article.product_pod', 'h3 a', 'p.price_color', 'h3 a', 50)
         """)
-        logger.info("Skapade default configs")
+        logger.info("Created default configs")
     
     conn.commit()
     return_db(conn)
@@ -126,7 +126,7 @@ def init_db():
 
 
 def load_configs():
-    """Ladda aktiva konfigurationer"""
+    """Load active configurations"""
     conn = get_db()
     cur = conn.cursor()
     cur.execute("SELECT * FROM scraper_config WHERE enabled = 1 ORDER BY name")
@@ -159,7 +159,7 @@ async def extract_product(page, element, config):
         
         return {'url': url, 'title': title[:200], 'price': price, 'site_config_id': config['id']}
     except Exception as e:
-        logger.debug(f"Extraheringsfel: {e}")
+        logger.debug(f"Extraction error: {e}")
         return None
 
 
@@ -194,7 +194,7 @@ async def scrape_site(context, config):
             
             elements = await page.query_selector_all(config['product_selector'])
-            logger.info(f"  Hittade {len(elements)} element")
+            logger.info(f"  Found {len(elements)} elements")
             
             for elem in elements:
                 product = await extract_product(page, elem, config)
@@ -220,9 +220,9 @@ async def scrape_site(context, config):
             page_num += 1
             await asyncio.sleep(random.uniform(3, 7))
         
-        logger.info(f"Klar med {config['name']}: {products_found} produkter")
+        logger.info(f"Done with {config['name']}: {products_found} products")
     except Exception as e:
-        logger.error(f"Fel i {config['name']}: {e}")
+        logger.error(f"Error in {config['name']}: {e}")
         stats['errors'] += 1
 
 
@@ -264,7 +264,7 @@ async def flush_buffer():
                 stats['products'] += 1
                 
         except Exception as e:
-            logger.error(f"DB-fel: {e}")
+            logger.error(f"DB error: {e}")
             stats['errors'] += 1
     
     conn.commit()
@@ -283,7 +283,7 @@ async def periodic_flush():
 async def run_scraper():
     configs = load_configs()
     if not configs:
-        logger.warning("Inga aktiva konfigurationer")
+        logger.warning("No active configurations")
         return
     
     flush_task = asyncio.create_task(periodic_flush())
@@ -332,7 +332,7 @@ async def run_scraper():
     if write_buffer:
         await flush_buffer()
     
-    logger.info(f"Klar. Nya: {stats['products']}, Uppdaterade: {stats['updated']}, Skippade: {stats['skipped']}")
+    logger.info(f"Done. New: {stats['products']}, Updated: {stats['updated']}, Skipped: {stats['skipped']}")
 
 
 async def scraper_loop():
@@ -342,7 +342,7 @@ async def scraper_loop():
         try:
             await run_scraper()
         except Exception as e:
-            logger.error(f"Scraping misslyckades: {e}")
+            logger.error(f"Scraping failed: {e}")
         finally:
             scraping_active = False
         
@@ -441,7 +441,7 @@ def export_site_csv(site_name):
     writer = csv.writer(output)
-    writer.writerow(['Produkt', 'Pris (SEK)', 'Länk'])
+    writer.writerow(['Product', 'Price (SEK)', 'Link'])
     
     for p in products:
         writer.writerow([p['title'], p['current_price'], p['url']])
@@ -456,7 +456,7 @@ def export_site_csv(site_name):
 
 
 def signal_handler(signum, frame):
-    logger.info(f"Signal {signum}, stänger ner...")
+    logger.info(f"Signal {signum}, shutting down...")
     shutdown_event.set()
 
 
diff --git a/webui/app.py b/webui/app.py
index 6789012..fghijkl 100644
--- a/webui/app.py
+++ b/webui/app.py
@@ -1,7 +1,7 @@
 #!/usr/bin/env python3
 # -*- coding: utf-8 -*-
 """
-WebUI Control Plane - Proxyrar anrop till API och Scraper Engine
+WebUI Control Plane - Proxies requests to API and Scraper Engine
 """
 
 import os
@@ -19,7 +19,7 @@ logging.basicConfig(level=logging.INFO)
 logger = logging.getLogger(__name__)
 
 def read_secret(env_var, default=""):
-    """Läs secret från fil eller env"""
+    """Read secret from file or env"""
     path = os.getenv(f"{env_var}_FILE")
     if path and os.path.exists(path):
         with open(path) as f:
@@ -85,7 +85,7 @@ def delete_config(config_id):
 @app.route('/api/scrape', methods=['POST'])
 def trigger_scrape():
     try:
-        # FIX: Anropa rätt endpoint på engine
+        # FIX: Call correct endpoint on engine
         resp = engine_request('POST', '/scrape')
         return jsonify(resp.json()), resp.status_code
     except Exception as e:
diff --git a/webui/templates/config.html b/webui/templates/config.html
index 7890123..ghijklm 100644
--- a/webui/templates/config.html
+++ b/webui/templates/config.html
@@ -1,7 +1,7 @@
 <!DOCTYPE html>
-<html lang="sv">
+<html lang="en">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Konfiguration - Web Scraper</title>
+    <title>Configuration - Web Scraper</title>
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
@@ -11,7 +11,7 @@
         <div class="container-fluid">
             <a class="navbar-brand" href="/">Web Scraper Pro</a>
             <div class="navbar-nav ms-auto">
                 <a class="nav-link" href="/">Dashboard</a>
-                <a class="nav-link active" href="/config">Konfiguration</a>
+                <a class="nav-link active" href="/config">Configuration</a>
             </div>
         </div>
     </nav>
@@ -19,17 +19,17 @@
     <div class="container mt-4">
         <div class="d-flex justify-content-between align-items-center mb-3">
-            <h1>Konfiguration</h1>
+            <h1>Configuration</h1>
             <button class="btn btn-primary" onclick="showAddForm()">
-                <i class="bi bi-plus-circle"></i> Lägg till ny site
+                <i class="bi bi-plus-circle"></i> Add new site
             </button>
         </div>
         
         <!-- Add Form (hidden by default) -->
         <div id="addForm" class="card mb-4" style="display: none;">
-            <div class="card-header">Lägg till ny scraper-konfiguration</div>
+            <div class="card-header">Add new scraper configuration</div>
             <div class="card-body">
                 <form onsubmit="addConfig(event)">
                     <div class="row">
                         <div class="col-md-6 mb-3">
-                            <label>Namn</label>
-                            <input type="text" class="form-control" id="cfgName" required placeholder="t.ex. Komplett.se">
+                            <label>Name</label>
+                            <input type="text" class="form-control" id="cfgName" required placeholder="e.g. Example.com">
                         </div>
                         <div class="col-md-6 mb-3">
                             <label>URL</label>
@@ -48,15 +48,15 @@
                     <div class="row">
                         <div class="col-md-6 mb-3">
-                            <label>Produkt-container</label>
+                            <label>Product container</label>
                             <input type="text" class="form-control" id="cfgProduct" required placeholder="div.product-item">
                         </div>
                         <div class="col-md-6 mb-3">
-                            <label>Titel-selektor</label>
+                            <label>Title selector</label>
                             <input type="text" class="form-control" id="cfgTitle" required placeholder="h2.product-title">
                         </div>
                     </div>
                     <div class="row">
                         <div class="col-md-6 mb-3">
-                            <label>Pris-selektor</label>
+                            <label>Price selector</label>
                             <input type="text" class="form-control" id="cfgPrice" required placeholder="span.price">
                         </div>
                         <div class="col-md-6 mb-3">
-                            <label>Länk-selektor</label>
+                            <label>Link selector</label>
                             <input type="text" class="form-control" id="cfgLink" required placeholder="a.product-link">
                         </div>
                     </div>
-                    <button type="submit" class="btn btn-success">Spara</button>
-                    <button type="button" class="btn btn-secondary" onclick="hideAddForm()">Avbryt</button>
+                    <button type="submit" class="btn btn-success">Save</button>
+                    <button type="button" class="btn btn-secondary" onclick="hideAddForm()">Cancel</button>
                 </form>
             </div>
         </div>
@@ -66,7 +66,7 @@
         <div class="card mb-4">
-            <div class="card-header">Snabbtemplates</div>
+            <div class="card-header">Quick Templates</div>
             <div class="card-body">
                 <button class="btn btn-outline-secondary me-2" onclick="loadTemplate('inet')">Inet.se</button>
                 <button class="btn btn-outline-secondary me-2" onclick="loadTemplate('komplett')">Komplett.se</button>
@@ -78,7 +78,7 @@
         <div class="card">
             <div class="card-header">
-                <h5 class="mb-0">Aktiva konfigurationer</h5>
+                <h5 class="mb-0">Active configurations</h5>
             </div>
             <div class="card-body">
-                <div id="configsList">Laddar...</div>
+                <div id="configsList">Loading...</div>
             </div>
         </div>
     </div>
@@ -129,7 +129,7 @@
             .then(r => r.json())
             .then(res => {
                 if (res.status === 'success') {
-                    alert('Konfiguration sparad!');
+                    alert('Configuration saved!');
                     hideAddForm();
                     loadConfigs();
-                    // Rensa formulär
+                    // Clear form
                     document.getElementById('cfgName').value = '';
@@ -140,11 +140,11 @@
                     document.getElementById('cfgLink').value = '';
                 } else {
-                    alert('Fel: ' + (res.message || 'Kunde inte spara'));
+                    alert('Error: ' + (res.message || 'Could not save'));
                 }
             })
-            .catch(err => alert('Fel: ' + err));
+            .catch(err => alert('Error: ' + err));
         }
 
         function deleteConfig(id) {
-            if (!confirm('Ta bort denna konfiguration?')) return;
+            if (!confirm('Delete this configuration?')) return;
             fetch(`/api/configs/${id}`, {method: 'DELETE'})
                 .then(r => r.json())
                 .then(() => loadConfigs())
-                .catch(err => alert('Fel: ' + err));
+                .catch(err => alert('Error: ' + err));
         }
 
@@ -157,7 +157,7 @@
                 .then(configs => {
                     if (configs.length === 0) {
                         document.getElementById('configsList').innerHTML = 
-                            '<p class="text-muted">Inga konfigurationer hittades.</p>';
+                            '<p class="text-muted">No configurations found.</p>';
                     } else {
-                        let html = '<table class="table"><thead><tr><th>Namn</th><th>URL</th><th>Aktiv</th><th></th></tr></thead><tbody>';
+                        let html = '<table class="table"><thead><tr><th>Name</th><th>URL</th><th>Active</th><th></th></tr></thead><tbody>';
                         configs.forEach(c => {
                             html += `<tr>
@@ -174,10 +174,10 @@
                 .catch(err => {
                     document.getElementById('configsList').innerHTML = 
-                        '<p class="text-danger">Kunde inte ladda konfigurationer</p>';
+                        '<p class="text-danger">Could not load configurations</p>';
                 });
         }
 
-        // Ladda vid start
+        // Load on start
         loadConfigs();
     </script>
 </body>
diff --git a/webui/templates/index.html b/webui/templates/index.html
index 8901234..hijklmn 100644
--- a/webui/templates/index.html
+++ b/webui/templates/index.html
@@ -1,7 +1,7 @@
 <!DOCTYPE html>
-<html lang="sv">
+<html lang="en">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
@@ -15,7 +15,7 @@
             <div class="navbar-nav ms-auto">
                 <a class="nav-link active" href="/">Dashboard</a>
-                <a class="nav-link" href="/config">Konfiguration</a>
+                <a class="nav-link" href="/config">Configuration</a>
             </div>
         </div>
     </nav>
@@ -28,17 +28,17 @@
             <div class="col-md-4">
                 <div class="card bg-primary text-white">
                     <div class="card-body">
-                        <h5>Produkter</h5>
+                        <h5>Products</h5>
                         <h2 id="total-products">-</h2>
                     </div>
                 </div>
             </div>
             <div class="col-md-4">
                 <div class="card bg-success text-white">
                     <div class="card-body">
-                        <h5>Uppdaterade (24h)</h5>
+                        <h5>Updated (24h)</h5>
                         <h2 id="updated-24h">-</h2>
                     </div>
                 </div>
             </div>
             <div class="col-md-4">
                 <div class="card bg-info text-white">
                     <div class="card-body">
-                        <h5>Aktiva konfigurationer</h5>
+                        <h5>Active configurations</h5>
                         <h2 id="active-configs">-</h2>
                     </div>
                 </div>
@@ -48,15 +48,15 @@
         <!-- Actions -->
         <div class="card mb-4">
-            <div class="card-header">Åtgärder</div>
+            <div class="card-header">Actions</div>
             <div class="card-body">
                 <button class="btn btn-primary" onclick="triggerScrape()">
-                    <i class="bi bi-play-fill"></i> Starta scraping nu
+                    <i class="bi bi-play-fill"></i> Start scraping now
                 </button>
                 <a href="/config" class="btn btn-secondary">
-                    <i class="bi bi-gear"></i> Konfiguration
+                    <i class="bi bi-gear"></i> Configuration
                 </a>
                 <button class="btn btn-success" onclick="exportData()">
-                    <i class="bi bi-download"></i> Exportera CSV
+                    <i class="bi bi-download"></i> Export CSV
                 </button>
             </div>
         </div>
@@ -65,15 +65,15 @@
         <div class="card">
             <div class="card-header d-flex justify-content-between">
-                <span>Senaste produkterna</span>
+                <span>Latest products</span>
                 <input type="text" id="searchInput" class="form-control form-control-sm w-25" 
-                       placeholder="Sök..." onkeyup="loadProducts()">
+                       placeholder="Search..." onkeyup="loadProducts()">
             </div>
             <div class="card-body p-0">
                 <table class="table table-hover mb-0">
                     <thead>
                         <tr>
-                            <th>Produkt</th>
-                            <th>Pris</th>
-                            <th>Senast uppdaterad</th>
+                            <th>Product</th>
+                            <th>Price</th>
+                            <th>Last updated</th>
                         </tr>
                     </thead>
                     <tbody id="productsTable">
-                        <tr><td colspan="3" class="text-center">Laddar...</td></tr>
+                        <tr><td colspan="3" class="text-center">Loading...</td></tr>
                     </tbody>
                 </table>
@@ -98,7 +98,7 @@
                     const tbody = document.getElementById('productsTable');
                     if (data.products.length === 0) {
-                        tbody.innerHTML = '<tr><td colspan="3" class="text-center">Inga produkter</td></tr>';
+                        tbody.innerHTML = '<tr><td colspan="3" class="text-center">No products</td></tr>';
                         return;
                     }
                     tbody.innerHTML = data.products.map(p => `
@@ -112,12 +112,12 @@
 
         function triggerScrape() {
-            if (!confirm('Starta scraping nu?')) return;
+            if (!confirm('Start scraping now?')) return;
             fetch('/api/scrape', { method: 'POST' })
                 .then(r => r.json())
-                .then(data => alert(data.message || 'Startad!'))
-                .catch(err => alert('Fel: ' + err));
+                .then(data => alert(data.message || 'Started!'))
+                .catch(err => alert('Error: ' + err));
         }
 
         function exportData() {
